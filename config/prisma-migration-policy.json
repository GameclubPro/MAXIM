{
  "schemaVersion": 1,
  "policyRulesAfter": "20260718183000_add_vk_rich_text_channel_link",
  "baselineThrough": "20260906090000_add_chat_rules_text_format",
  "baselineNamesSha256": "7ee433bb1aeebbc7f16b9fb2c84f10baaf73d5ee3f0263f7a90f29282030865d",
  "baselineContentsSha256": "a8c87889e4b2b7b6ee9de9d05103f4ad6d2fac5db8997065fef493dd718115d8",
  "approvedDestructive": {
    "20260301150000_drop_commercial_phrase_tables": {
      "reason": "Historical migration recorded before the executable policy guard."
    },
    "20260302012000_remove_unmanaged_chat_settings_fields": {
      "reason": "Historical migration recorded before the executable policy guard."
    },
    "20260310185000_remove_channel_comment_slow_mode": {
      "reason": "Historical migration recorded before the executable policy guard."
    },
    "20260310223000_remove_legacy_thematic_topic_filters": {
      "reason": "Historical migration recorded before the executable policy guard."
    },
    "20260314090000_global_spammers_and_remove_legacy_cross_chat_blacklist": {
      "reason": "Historical migration recorded before the executable policy guard."
    },
    "20260320003000_move_comment_scope_toggles_to_chat_settings": {
      "reason": "Historical migration recorded before the executable policy guard."
    },
    "20260325113000_drop_admin_profile_preferences": {
      "reason": "Historical migration recorded before the executable policy guard."
    },
    "20260620175000_remove_managed_polls": {
      "reason": "Historical migration recorded before the executable policy guard."
    },
    "20260716050000_drop_thematic_codeword_settings": {
      "reason": "Historical migration recorded before the executable policy guard."
    },
    "20260809123000_drop_channel_auto_post_buttons_mode": {
      "reason": "Remove the retired channel auto-post button mode after every production API role stopped selecting the column.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "Compatible runtime SHA 38fc559155500934773e20b291c9498a6cae29ba was deployed as release release-20260809T020524Z-38fc55915550 and verified live on all 11 production API roles before this migration."
    },
    "20260811120000_add_link_history_recovery_state": {
      "reason": "Manual review approved two static trigger functions that maintain non-retroactive link-policy recovery baselines. The policy parser treats CREATE TRIGGER EXECUTE FUNCTION as dynamic and destructive, but this migration contains no dynamic EXECUTE and drops no schema objects.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "The parser's drop-column classification is a false positive from EXECUTE FUNCTION. The migration is additive: existing runtime continues to use chat_settings while new columns have compatible defaults or are nullable, and the new scan-state table has no existing consumers."
    },
    "20260811123000_align_link_history_timestamp_types": {
      "reason": "Manual review approved an atomic drop-and-recreate of the chat settings recovery trigger because PostgreSQL blocks changing the type of a column named by a trigger definition. The migration restores the same trigger and function before commit, and it drops no table columns.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "The parser's drop-column classification is a false positive from CREATE TRIGGER EXECUTE FUNCTION. Existing and new runtimes both expose these columns as Prisma DateTime values; the transaction preserves UTC instants, removes only the unintended updated_at default, and cannot commit without restoring the trigger."
    },
    "20260821122000_guard_channel_suggestion_publication_ledger_insert": {
      "reason": "Manual review approved an additive BEFORE INSERT trigger that serializes the exact versioned channel-suggestion ledger signature with stale claim release. The policy parser treats CREATE TRIGGER EXECUTE FUNCTION as dynamic and destructive, but this migration contains no dynamic EXECUTE and drops no schema objects.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "Existing and new runtimes create versioned suggestion ledgers only while the exact publishing claim is active; all other max_action_ledger signatures bypass the trigger. After claim finalization, only a row-locked exact terminal-success conflict may reach the caller's ON CONFLICT no-op; every genuinely new row still requires the active claim. The migration does not change stored columns or reads."
    },
    "20260821130000_add_night_mode_transition_reconcile_requests": {
      "reason": "Manual review approved static trigger functions that enqueue generation-fenced night-mode reconciliation requests. The policy parser treats CREATE TRIGGER EXECUTE FUNCTION bodies as dynamic and destructive, but this additive migration contains no dynamic EXECUTE and drops no schema object.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "The new tables, functions, indexes, and triggers are additive. Existing runtimes ignore the outbox while its triggers only upsert rows in the new reconcile table; the new runtime starts consuming those rows after the migration completes. DateTime columns use Prisma's PostgreSQL TIMESTAMP(3) mapping, and the scheduled-job writer supplies both required audit timestamps instead of relying on an undeclared updated_at database default. No existing column, constraint, or data contract changes."
    },
    "20260826120000_add_publisher_dispatch_foundation": {
      "reason": "Manual review approved static parent-route propagation and immutability triggers plus the bounded prepared-dialog-context validator. The policy parser treats CREATE TRIGGER EXECUTE FUNCTION and PL/pgSQL bodies as dynamic and destructive, but this additive migration contains no dynamic EXECUTE and drops no schema object.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "The migration adds new tables and columns with legacy-compatible defaults. Existing runtimes continue to create LEGACY_ROUTED roots and envelopes; new occurrence, broadcast, and delivery rows derive immutable route fields from their parent even when a mixed-version writer supplies legacy defaults. New PUBLIK_V1 roots are enabled only after every API role runs compatible code."
    },
    "20260826122000_add_vk_publisher_dispatch": {
      "reason": "Manual review approved one static BEFORE UPDATE trigger that prevents an active VK publish intent from changing its exact dispatch route. The policy parser treats CREATE TRIGGER EXECUTE FUNCTION and UPDATE OF as dynamic/destructive SQL, but this additive migration contains no dynamic EXECUTE and drops no schema object or column.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "All added VK columns are nullable or use LEGACY_ROUTED-compatible defaults, so existing queued jobs and mixed-version action workers retain their main-bot route. The new runtime stamps PUBLIK_V1 only for newly armed intents after publisher readiness succeeds and stores the main-signed dialog context before handing work to the token-isolated publisher runtime. Concurrent indexes avoid blocking the existing VK post workload, and the trigger preserves route and bot provenance for active, published, and rollback-bound intents."
    },
    "20260826123000_add_publisher_dialog_context": {
      "reason": "Manual review approved one static delivery-route immutability trigger. The policy parser treats CREATE TRIGGER EXECUTE FUNCTION as dynamic and destructive, but this additive migration contains no dynamic EXECUTE and drops no schema object or column.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "The migration adds one nullable JSONB column to an existing delivery table, validates existing LEGACY_ROUTED rows through a lower-lock NOT VALID plus VALIDATE sequence, and requires a bounded main-signed context only for future PUBLIK_V1 deliveries. Old runtime cannot create PUBLIK_V1 routes, and publisher dispatch remains disabled until every API role runs the compatible client."
    },
    "20260829130000_restrict_vk_parsing_to_publisher": {
      "reason": "Delete retired Major-owned VK settings, sources, and posts, including cross-owner child posts attached to a Major source, then make every persisted VK owner scope Publisher-only with an explicit nonblank bot id.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "Compatible runtime release a5ef2537ebb96bde30ce30a5c33586d41248d8ca was deployed and verified on every production API role before this migration; it no longer creates, recovers, schedules, or publishes Major-owned VK parsing work and supplies explicit Publisher ownership on every surviving write."
    },
    "20260902120000_add_publisher_auto_reply_matchers": {
      "reason": "Manual review approved two static trigger functions that validate Publisher auto-reply trigger ownership and mirror the legacy primary phrase plus archive state, plus an atomic widening of the authoring-message kind check for durable callback deduplication. The policy parser treats CREATE TRIGGER EXECUTE FUNCTION, PL/pgSQL bodies, UPDATE OF, and the check-constraint replacement as dynamic or destructive, but this migration contains no dynamic EXECUTE and drops no schema object, column, or accepted value.",
      "twoPhaseRelease": true,
      "runtimeCompatibilityEvidence": "Every existing-table column is nullable or has an exact-match-compatible constant default, all legacy phrase columns and indexes remain present, existing authoring PHRASE and CONTENT rows remain accepted, and existing deliveries retain EXACT_FULL distance-zero snapshots. The explicit PostgreSQL transaction holds the parent-rule write lock from the additive ALTER through the backfill and trigger installation, so the one position-zero trigger per legacy rule and the database mirror remain aligned with mixed-version writers throughout a rolling deployment."
    }
  },
  "nonConcurrentIndexExceptions": {
    "20260824090000_add_karavan_storefront_admin_allowlist": {
      "reason": "The indexes are created immediately after the new empty allowlist table; keeping them in the same atomic additive migration avoids exposing a table without its uniqueness and lookup indexes, and there is no existing workload to block."
    },
    "20260821130000_add_night_mode_transition_reconcile_requests": {
      "reason": "The index is created immediately after its new empty table and before the bounded enabled-chat backfill, so there is no existing table workload to block and PostgreSQL forbids CREATE INDEX CONCURRENTLY inside Prisma's migration transaction."
    },
    "20260826120000_add_publisher_dispatch_foundation": {
      "reason": "All non-concurrent indexes in this migration are created on the two new empty publisher policy/binding tables before they receive rows. Existing publication-table claim indexes are isolated in the following concurrent-index migration."
    },
    "20260828130000_add_publisher_post_import_sessions": {
      "reason": "All indexes are created with the new empty post-import session table before it can receive rows. Keeping its active-session uniqueness and recovery indexes atomic avoids exposing an unfenced capture state, and there is no existing table workload to block."
    },
    "20260829120000_add_publisher_auto_replies": {
      "reason": "All non-concurrent indexes are created with new empty auto-reply, asset, delivery, authoring-message, and lease tables. Keeping partial uniqueness, draft provenance, deduplication, and recovery indexes atomic prevents unfenced matcher or authoring states, and the only existing-table change is an additive boolean column with a constant default."
    },
    "20260902120000_add_publisher_auto_reply_matchers": {
      "reason": "Trigger uniqueness and lookup indexes are created on the new table in the same atomic migration as its bounded one-row-per-rule backfill. The redundant rule id/chat unique index is required immediately by the composite ownership foreign key, and the nullable delivery provenance index keeps trigger replacement from scanning the delivery ledger; these Publisher auto-reply tables have bounded recent product history."
    }
  }
}
